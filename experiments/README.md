# Experiments

This directory contains the executable entry points for running the XGB Benchmark experiments.

Reusable experiment logic is implemented in `src/benchmark`, while experiment settings are defined in `configs`.
